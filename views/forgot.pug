extends layout

block content
  - if (message)
  h1 #{message}
  .main.container
    .row
      .col-md-6.col-md-offset-3
        h1.display-4.m-b-2 Reset your password

        form(name="regis" method='POST' action='/forgotPass')
          div.form-group
            label(for='email') Email:
            input#email.form-control(type='email', placeholder='name@example.com' name='email')
          button.btn.btn-primary(type='submit') Send email
  a.foo_link(href='/') Return to the login page
